= dotfiles

forked from ryanb/dotfiles and customized for my own purpose

== Installation

  git clone --recursive git://github.com/pboehm/dotfiles ~/.dotfiles
  cd ~/.dotfiles
  ./install.sh     # and install the missing programs if needed

